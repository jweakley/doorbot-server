%h1 Dashboard
.container-fluid
  .container-fluid
    %h2 Doorbot Server Software
    #version-badge Checking Version...
  .container-fluid
    %h2 Doorbots
    -if @doorbots.any?
      %li
        - @doorbots.each do |doorbot|
          %ul=link_to doorbot.nickname, doorbot
    -else
      %p No doorbots! Add some #{link_to('here', new_doorbot_url)}

